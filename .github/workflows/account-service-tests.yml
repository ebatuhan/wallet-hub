name: Account Service Tests

on:
  pull_request:
    paths:
      - "services/account-service/**"
      - "services/shared/**"
      - ".github/workflows/account-service-tests.yml"
  push:
    branches:
      - dev
      - main
    paths:
      - "services/account-service/**"
      - "services/shared/**"
      - ".github/workflows/account-service-tests.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    name: Test account service
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven

      - name: Install shared module
        run: mvn --batch-mode --no-transfer-progress -f services/shared/pom.xml clean install

      - name: Verify account service
        run: mvn --batch-mode --no-transfer-progress -f services/account-service/pom.xml clean verify

      - name: Upload Surefire reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: account-service-surefire-reports
          path: services/account-service/target/surefire-reports/
          if-no-files-found: error

      - name: Upload Failsafe reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: account-service-failsafe-reports
          path: services/account-service/target/failsafe-reports/
          if-no-files-found: error
