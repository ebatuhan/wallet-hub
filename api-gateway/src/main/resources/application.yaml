spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        - id: jsonplaceholder-route
          uri: https://jsonplaceholder.typicode.com
          predicates:
            - Path=/api/**
          filters:
            - StripPrefix=1

server:
  port: ${API_GATEWAY_PORT}
